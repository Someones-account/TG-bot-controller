# This file (for now) serves just to show table schema, without actually representing it in code

# class ModerationActions():
#     int id;  unique entry id
#     BIGINT user_id;  Telegram user id
#     String action;   Action performed by a bot
#     Timestamp timestamp;   Issue date
#     Timestamp lift_time;   Lift date
